# Postgres (DB) — used by both db and api
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=appdb

# API DB URL — the hostname is **db** (the service name), not localhost
DATABASE_URL=postgresql://postgres:postgres@db:5432/appdb

# Node
NODE_ENV=production
PORT=3000
